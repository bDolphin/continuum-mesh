# Web API
fastapi
uvicorn[standard]

# Config & validation
pydantic
python-dotenv

# Vector math (persistent SQLite store does cosine in numpy)
numpy

# --- Local-first embeddings: install ONE of these ---
# Best quality (needs torch; use Python 3.12 if torch has no 3.14 wheel yet):
sentence-transformers
# No-torch fallback, installs cleanly on Python 3.14:
model2vec

# HTTP + embeddings (OpenAI mode, optional)
httpx
openai

# Utilities
orjson
loguru

# Accurate token counting for the context assembler (Cycle 5). The assembler
# falls back to a ~4-chars/token heuristic if this is missing, but tiktoken
# is what makes the budget tight and Unicode-correct.
tiktoken

# NOTE: chromadb was removed from the hot path. The SQLite store in
# memory_store.py persists locally with zero external services and works on
# Python 3.14. To graduate to Chroma/Qdrant later, implement the same
# MemoryStore interface behind a feature flag — the daemon won't need changes.
